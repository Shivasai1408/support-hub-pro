import { Link, useNavigate } from "@tanstack/react-router";
import { Bell, LogOut, Plus, Search } from "lucide-react";
import { useEffect, useState, type ReactNode } from "react";
import { AppSidebar } from "@/components/app-sidebar";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Input } from "@/components/ui/input";
import { SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar";
import { useSupport } from "@/lib/support-store";
export function AppShell({title,description,actions,children,contentClassName}:{title:string;description?:string;actions?:ReactNode;children:ReactNode;contentClassName?:string}){const {user,signOut,tickets}=useSupport();const navigate=useNavigate();const [search,setSearch]=useState("");const unrouted=tickets.filter(t=>!t.assigneeId&&t.status==="open").length;useEffect(()=>{if(!user)navigate({to:"/auth"})},[user,navigate]);if(!user)return null;return <SidebarProvider><div className="flex min-h-screen w-full bg-background"><AppSidebar/><div className="flex min-w-0 flex-1 flex-col"><header className="sticky top-0 z-20 flex h-16 items-center gap-3 border-b border-border bg-card/85 px-3 backdrop-blur sm:px-5"><SidebarTrigger aria-label="Toggle navigation"/><div className="relative hidden max-w-xs flex-1 md:block"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><Input type="search" value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&search.trim()){navigate({to:"/inbox"});setSearch("")}}} placeholder="Search tickets, customers…" aria-label="Search" className="pl-9"/></div><div className="ml-auto flex items-center gap-1.5 sm:gap-2"><Button asChild size="sm" className="hidden sm:inline-flex"><Link to="/inbox"><Plus className="size-4"/>New ticket</Link></Button><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" aria-label="Notifications" className="relative"><Bell className="size-4"/>{unrouted>0&&<span className="absolute right-1.5 top-1.5 size-2 rounded-full bg-destructive"/>}</Button></DropdownMenuTrigger><DropdownMenuContent align="end" className="w-72"><DropdownMenuLabel>Notifications</DropdownMenuLabel><DropdownMenuSeparator/><DropdownMenuItem asChild><Link to="/inbox" className="flex-col items-start gap-0.5"><span className="text-sm font-medium">{unrouted} tickets need routing</span><span className="text-xs text-muted-foreground">Open the inbox to assign them</span></Link></DropdownMenuItem><DropdownMenuItem asChild><Link to="/analytics" className="flex-col items-start gap-0.5"><span className="text-sm font-medium">CSAT 4.6 this week</span><span className="text-xs text-muted-foreground">View support performance</span></Link></DropdownMenuItem></DropdownMenuContent></DropdownMenu><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" className="gap-2 px-1.5 sm:px-2"><Avatar className="size-7"><AvatarFallback className="bg-primary/12 text-xs font-semibold text-primary">{user.initials}</AvatarFallback></Avatar><span className="hidden text-sm font-medium lg:inline">{user.name}</span></Button></DropdownMenuTrigger><DropdownMenuContent align="end" className="w-56"><DropdownMenuLabel className="flex flex-col"><span>{user.name}</span><span className="text-xs font-normal text-muted-foreground">{user.email}</span></DropdownMenuLabel><DropdownMenuSeparator/><DropdownMenuItem asChild><Link to="/settings">Workspace settings</Link></DropdownMenuItem><DropdownMenuItem onSelect={()=>signOut()}><LogOut className="size-4"/>Sign out</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></header><main className={contentClassName??"flex-1 px-3 py-5 sm:px-6 sm:py-7"}><div className="mx-auto w-full max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="font-display text-2xl font-semibold sm:text-[28px]">{title}</h1>{description&&<p className="mt-1 text-sm text-muted-foreground">{description}</p>}</div>{actions&&<div className="flex flex-wrap items-center gap-2">{actions}</div>}</div><div className="mt-6">{children}</div></div></main></div></div></SidebarProvider>}
