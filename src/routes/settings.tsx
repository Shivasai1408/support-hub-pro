import { createFileRoute } from "@tanstack/react-router";
import { Bell, Bot, Save, Shield, User } from "lucide-react";
import { useState } from "react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { useSupport } from "@/lib/support-store";

export const Route = createFileRoute("/settings")({ component: SettingsPage });
function Toggle({label,description,value,onChange}:{label:string;description:string;value:boolean;onChange:(v:boolean)=>void}){return <button onClick={()=>onChange(!value)} className="flex w-full items-center justify-between gap-4 rounded-lg border p-4 text-left hover:bg-accent/40"><span><span className="block text-sm font-medium">{label}</span><span className="mt-1 block text-xs text-muted-foreground">{description}</span></span><span className={`relative h-6 w-11 rounded-full ${value?"bg-primary":"bg-muted"}`}><span className={`absolute top-1 size-4 rounded-full bg-white transition-transform ${value?"translate-x-6":"translate-x-1"}`}/></span></button>}
function SettingsPage(){const {settings,updateSettings,user}=useSupport();const [name,setName]=useState(user?.name??"");const [saved,setSaved]=useState(false);const save=()=>{updateSettings({});setSaved(true);setTimeout(()=>setSaved(false),1800)};return <AppShell title="Settings" description="Configure your workspace, AI automation and notification preferences."><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2"><User className="size-5 text-primary"/>Profile</CardTitle></CardHeader><CardContent className="space-y-4"><div><label className="text-sm font-medium">Name</label><Input className="mt-2" value={name} onChange={e=>setName(e.target.value)}/></div><div><label className="text-sm font-medium">Email</label><Input className="mt-2" value={user?.email??""} disabled/></div><Button onClick={save}><Save className="size-4"/>{saved?"Saved":"Save changes"}</Button></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Bot className="size-5 text-primary"/>AI automation</CardTitle></CardHeader><CardContent className="space-y-2"><Toggle label="Auto-draft replies" description="Draft responses from ticket context and your knowledge base." value={settings.autoDraft} onChange={v=>updateSettings({autoDraft:v})}/><Toggle label="Auto-classify tickets" description="Automatically classify topic, priority and routing." value={settings.autoClassify} onChange={v=>updateSettings({autoClassify:v})}/><Toggle label="Auto-resolve simple tickets" description="Allow high-confidence AI answers to resolve simple requests." value={settings.autoResolveSimple} onChange={v=>updateSettings({autoResolveSimple:v})}/><div className="rounded-lg border p-4"><div className="flex justify-between text-sm"><span>Confidence threshold</span><strong>{settings.confidenceThreshold}%</strong></div><input type="range" min="50" max="100" value={settings.confidenceThreshold} onChange={e=>updateSettings({confidenceThreshold:Number(e.target.value)})} className="mt-3 w-full"/></div></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Bell className="size-5 text-primary"/>Notifications</CardTitle></CardHeader><CardContent className="space-y-2"><Toggle label="Escalations" description="Notify me when urgent tickets need attention." value={settings.notifyEscalations} onChange={v=>updateSettings({notifyEscalations:v})}/><Toggle label="Daily digest" description="Receive a summary of support activity." value={settings.notifyDigest} onChange={v=>updateSettings({notifyDigest:v})}/><Toggle label="CSAT alerts" description="Notify me about new customer satisfaction feedback." value={settings.notifyCsat} onChange={v=>updateSettings({notifyCsat:v})}/></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Shield className="size-5 text-primary"/>Workspace</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><div className="flex justify-between border-b pb-3"><span className="text-muted-foreground">Workspace</span><span className="font-medium">Helm Support</span></div><div className="flex justify-between border-b pb-3"><span className="text-muted-foreground">AI tone</span><span className="font-medium capitalize">{settings.tone}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Authentication</span><span className="font-medium">Email sign-in</span></div></CardContent></Card></div></AppShell>}
